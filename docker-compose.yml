version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: cal-clone-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: cal_db
    ports:
      - "5432:5432"